<div class="container text-center center">
  <div class="row">

     <h1>Articles</h1>

     <ul>
      <% @articles.each do |article| %>
      <li><%= link_to(article.title, article) %> - <%= article.context %></li>
      <% end %>
    </ul>

    <%= link_to "New Article", new_article_path  %>

</div>
</div>
